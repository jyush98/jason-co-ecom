import Gallery from "./Gallery";

export default function GalleryPage() {
    return (
        <main className="min-h-screen bg-white py-5 dark:bg-black pt-[var(--navbar-height)]">
            <Gallery />
        </main>
    );
}
